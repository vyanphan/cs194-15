#include <cstdlib>
#include <cstdio>
#include <cstring>
#include <time.h>
#include <"counters.h">

int main(int argc, char *argv[]) {
    int n = atoi(argv[1]);
 
    long long sum = 0;   
    for(long long i=0; i<n; i++) {
        sum += i;
    }
}
